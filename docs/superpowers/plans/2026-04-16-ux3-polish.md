# UX3 — Полировка интерфейса: 6 улучшений

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Полировка UX: случайные цвета тегов, умные метки дат, фиксированный drag-preview, анимации аккордеонов и мелкие правки вёрстки.

**Architecture:** Все изменения клиентские — компоненты и хуки. Единственный новый файл — `TaskDragPreview` + `src/lib/dates.ts` для тестируемой функции форматирования. API и БД не затронуты.

**Tech Stack:** Next.js 16 App Router, React 19, Tailwind CSS 4, @dnd-kit/core + @dnd-kit/sortable, Vitest

---

## File Map

| Действие | Файл |
|----------|------|
| Modify | `src/hooks/useTags.ts` |
| Modify | `src/components/filters/DateFilters.tsx` |
| Modify | `src/app/tasks/page.tsx` |
| Modify | `src/components/tasks/TaskItem.tsx` |
| Modify | `src/components/tasks/TaskList.tsx` |
| Create | `src/lib/dates.ts` |
| Create | `src/lib/dates.test.ts` |
| Create | `src/components/tasks/TaskDragPreview.tsx` |
| Modify | `src/components/SortableTask.tsx` |
| Modify | `src/components/projects/ProjectTabs.tsx` |
| Modify | `src/components/filters/TagFilter.tsx` |

---

## Task 1: Случайные цвета тегов

**Files:**
- Modify: `src/hooks/useTags.ts`

- [ ] **Step 1: Добавить палитру и функцию случайного выбора**

Открыть `src/hooks/useTags.ts`. После строки `"use client"` добавить перед хуком:

```ts
const TAG_COLORS = [
  "#60a5fa", // blue-400
  "#a78bfa", // violet-400
  "#f472b6", // pink-400
  "#fb923c", // orange-400
  "#2dd4bf", // teal-400
  "#4ade80", // green-400
  "#fb7185", // rose-400
  "#22d3ee", // cyan-400
  "#c084fc", // purple-400
  "#94a3b8", // slate-400
]

function randomTagColor(): string {
  return TAG_COLORS[Math.floor(Math.random() * TAG_COLORS.length)]
}
```

- [ ] **Step 2: Использовать случайный цвет по умолчанию в createTag**

В `createTag` заменить первые строки тела функции:

```ts
const createTag = useCallback(
  async (name: string, color?: string): Promise<Tag> => {
    const tagColor = color ?? randomTagColor()
    const res = await fetch("/api/tags", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ name, color: tagColor }),
    })
    if (!res.ok) throw new Error("Не удалось создать метку")
    const tag = await res.json()
    setTags((prev) => [...prev, tag].sort((a, b) => a.name.localeCompare(b.name)))
    return tag
  },
  []
)
```

- [ ] **Step 3: Проверить в браузере**

Открыть приложение, развернуть задачу, открыть тег-пикер, создать 3-4 новых тега — каждый должен получить случайный цвет из палитры (не серый `#6b7280`).

- [ ] **Step 4: Commit**

```bash
git add src/hooks/useTags.ts
git commit -m "feat: random color from Tailwind-400 palette on tag creation"
```

---

## Task 2: Фильтр по дате — порядок табов и позиция

**Files:**
- Modify: `src/components/filters/DateFilters.tsx`
- Modify: `src/app/tasks/page.tsx`

- [ ] **Step 1: Изменить порядок табов в DateFilters**

В `src/components/filters/DateFilters.tsx` заменить массив `FILTERS`:

```ts
const FILTERS: DateFilter[] = ["today", "week", "someday", "all"]
```

Было: `["today", "week", "all", "someday"]`. Теперь «Потом» идёт перед «Все».

- [ ] **Step 2: Переместить DateFilters выше ProjectTabs в page.tsx**

В `src/app/tasks/page.tsx` найти блок с `<ProjectTabs>` и `<DateFilters>` и переставить их:

```tsx
<DateFilters value={dateFilter} onChange={setDateFilter} />

<ProjectTabs
  projects={projectHook.projects}
  activeProjectId={activeProjectId}
  onSelect={setActiveProjectId}
  onCreate={projectHook.createProject}
  onDelete={async (id) => {
    await projectHook.deleteProject(id)
    taskHook.removeProjectTasks(id)
    if (activeProjectId === id) setActiveProjectId(null)
  }}
  onUpdate={async (id, updates) => {
    const updated = await projectHook.updateProject(id, updates)
    if (updates.title) taskHook.syncProjectRename(updated)
  }}
/>

<TagFilter
  tags={tagHook.tags}
  selectedIds={selectedTagIds}
  onChange={setSelectedTagIds}
/>
```

`<DateFilters>` теперь первый под заголовком. `<TagFilter>` остаётся под проектами.

- [ ] **Step 3: Проверить в браузере**

Порядок табов сверху: «Сегодня | Неделя | Потом | Все». DateFilters — первый блок после «Мои задачи».

- [ ] **Step 4: Commit**

```bash
git add src/components/filters/DateFilters.tsx src/app/tasks/page.tsx
git commit -m "feat: reorder date filter tabs and move DateFilters above ProjectTabs"
```

---

## Task 3: Высота задач и плотность списка

**Files:**
- Modify: `src/components/tasks/TaskItem.tsx`
- Modify: `src/components/tasks/TaskList.tsx`

- [ ] **Step 1: Увеличить высоту collapsed-строки в TaskItem**

В `src/components/tasks/TaskItem.tsx` найти строку collapsed-row (строка ~183):

```tsx
<div className={`flex gap-3 px-3 py-3 ${isOpen ? "items-start" : "items-center"}`}>
```

Заменить `py-3` на `py-4`:

```tsx
<div className={`flex gap-3 px-3 py-4 ${isOpen ? "items-start" : "items-center"}`}>
```

- [ ] **Step 2: Уменьшить gap между задачами в TaskList**

В `src/components/tasks/TaskList.tsx` найти строку:

```tsx
<ul id="task-list" className="flex flex-col gap-2">
```

Заменить `gap-2` на `gap-1`:

```tsx
<ul id="task-list" className="flex flex-col gap-1">
```

- [ ] **Step 3: Проверить в браузере**

Пункты списка заметно выше (≈56px вместо 48px), расстояние между ними ≈4px вместо 8px.

- [ ] **Step 4: Commit**

```bash
git add src/components/tasks/TaskItem.tsx src/components/tasks/TaskList.tsx
git commit -m "feat: increase task row height (py-4) and tighten list gap (gap-1)"
```

---

## Task 4: Умные метки дат (сегодня / завтра / просрочено)

**Files:**
- Create: `src/lib/dates.ts`
- Create: `src/lib/dates.test.ts`
- Modify: `src/components/tasks/TaskItem.tsx`

- [ ] **Step 1: Написать падающий тест**

Создать `src/lib/dates.test.ts`:

```ts
import { describe, it, expect, beforeEach, afterEach, vi } from "vitest"
import { formatDueDate } from "./dates"

describe("formatDueDate", () => {
  beforeEach(() => {
    vi.useFakeTimers()
    // Фиксируем "сейчас" = 2026-04-20 12:00 местного времени
    vi.setSystemTime(new Date(2026, 3, 20, 12, 0, 0))
  })

  afterEach(() => {
    vi.useRealTimers()
  })

  it("возвращает 'просрочено' для прошедших дат", () => {
    expect(formatDueDate(new Date(2026, 3, 19).toISOString())).toBe("просрочено")
  })

  it("возвращает 'сегодня' для сегодняшней даты", () => {
    expect(formatDueDate(new Date(2026, 3, 20).toISOString())).toBe("сегодня")
  })

  it("возвращает 'завтра' для завтрашней даты", () => {
    expect(formatDueDate(new Date(2026, 3, 21).toISOString())).toBe("завтра")
  })

  it("возвращает локализованную дату для будущего", () => {
    expect(formatDueDate(new Date(2026, 3, 25).toISOString())).toBe("25.04.2026")
  })
})
```

- [ ] **Step 2: Запустить тест — убедиться что падает**

```bash
npx vitest run src/lib/dates.test.ts
```

Ожидаемый результат: FAIL (модуль `./dates` не существует).

- [ ] **Step 3: Реализовать formatDueDate**

Создать `src/lib/dates.ts`:

```ts
export function formatDueDate(dueDate: string): string {
  const today = new Date()
  today.setHours(0, 0, 0, 0)

  const tomorrow = new Date(today)
  tomorrow.setDate(today.getDate() + 1)

  const due = new Date(dueDate)
  due.setHours(0, 0, 0, 0)

  if (due < today) return "просрочено"
  if (due.getTime() === today.getTime()) return "сегодня"
  if (due.getTime() === tomorrow.getTime()) return "завтра"
  return due.toLocaleDateString("ru-RU")
}
```

- [ ] **Step 4: Запустить тест — убедиться что проходит**

```bash
npx vitest run src/lib/dates.test.ts
```

Ожидаемый результат: 4 тестов PASS.

- [ ] **Step 5: Использовать formatDueDate в TaskItem**

В `src/components/tasks/TaskItem.tsx` добавить импорт в начало:

```ts
import { formatDueDate } from "@/lib/dates"
```

Найти **collapsed badge** (~строка 242):

```tsx
{new Date(task.dueDate).toLocaleDateString("ru-RU")}
```

Заменить на:

```tsx
{formatDueDate(task.dueDate)}
```

Найти **expanded date label** (~строка 472):

```tsx
{task.dueDate
  ? new Date(task.dueDate).toLocaleDateString("ru-RU")
  : "Добавить дату"}
```

Заменить на:

```tsx
{task.dueDate ? formatDueDate(task.dueDate) : "Добавить дату"}
```

- [ ] **Step 6: Проверить в браузере**

Задачи с сегодняшней датой показывают «сегодня», с завтрашней — «завтра», с прошедшей — «просрочено» (красный фон из `dateBadgeClasses` остаётся).

- [ ] **Step 7: Commit**

```bash
git add src/lib/dates.ts src/lib/dates.test.ts src/components/tasks/TaskItem.tsx
git commit -m "feat: smart due date labels (сегодня/завтра/просрочено)"
```

---

## Task 5: Компонент TaskDragPreview

**Files:**
- Create: `src/components/tasks/TaskDragPreview.tsx`

- [ ] **Step 1: Создать компонент**

Создать `src/components/tasks/TaskDragPreview.tsx`:

```tsx
"use client"

import type { Task } from "@/types"
import { Check } from "lucide-react"

function priorityColor(score: number): string {
  const r = Math.round(59 + (229 - 59) * (1 - score))
  const g = Math.round(130 + (231 - 130) * (1 - score))
  const b = Math.round(246 + (235 - 246) * (1 - score))
  return `rgb(${r}, ${g}, ${b})`
}

export function TaskDragPreview({ task }: { task: Task }) {
  return (
    <div
      className="bg-white rounded-xl select-none overflow-hidden"
      style={{
        borderLeft: `3px solid ${priorityColor(task.priorityScore)}`,
        transform: "scale(1.03)",
        boxShadow: "0 8px 24px rgba(0,0,0,0.12)",
      }}
    >
      <div className="flex gap-3 px-3 py-4 items-center">
        <div
          className={`w-5 h-5 rounded-full border-2 flex items-center justify-center flex-shrink-0 ${
            task.done ? "border-blue-500 bg-blue-500" : "border-gray-300"
          }`}
        >
          {task.done && <Check className="w-3 h-3 text-white" strokeWidth={3} />}
        </div>
        <span className="text-sm font-medium text-gray-900 truncate flex-1">
          {task.title}
        </span>
      </div>
    </div>
  )
}
```

Компонент не имеет состояния и не подписывается ни на какие хуки — только отображает collapsed-строку задачи с анимацией scale.

- [ ] **Step 2: Commit**

```bash
git add src/components/tasks/TaskDragPreview.tsx
git commit -m "feat: TaskDragPreview — collapsed card for drag overlay"
```

---

## Task 6: DragOverlay — фиксированная высота при перетаскивании

**Files:**
- Modify: `src/app/tasks/page.tsx`
- Modify: `src/components/SortableTask.tsx`

- [ ] **Step 1: Добавить DragOverlay в page.tsx**

В `src/app/tasks/page.tsx` добавить `DragOverlay` в импорт из `@dnd-kit/core`:

```ts
import {
  DndContext,
  DragEndEvent,
  DragStartEvent,
  DragOverlay,
  MouseSensor,
  TouchSensor,
  useSensor,
  useSensors,
} from "@dnd-kit/core"
```

Добавить импорт компонента:

```ts
import { TaskDragPreview } from "@/components/tasks/TaskDragPreview"
import type { Task, DateFilter } from "@/types"
```

- [ ] **Step 2: Добавить состояние draggingTask**

В `TasksPage` после существующих `useState`:

```ts
const [draggingTask, setDraggingTask] = useState<Task | null>(null)
```

- [ ] **Step 3: Обновить handleDragStart**

Заменить существующую функцию `handleDragStart`:

```ts
function handleDragStart(event: DragStartEvent) {
  const task = taskHook.tasks.find((t) => t.id === event.active.id) ?? null
  setDraggingTask(task)
}
```

- [ ] **Step 4: Очищать draggingTask в handleDragEnd**

В начало `handleDragEnd` добавить:

```ts
async function handleDragEnd(event: DragEndEvent) {
  setDraggingTask(null)
  const { active, over } = event
  // ... остальной код без изменений
```

- [ ] **Step 5: Добавить DragOverlay в JSX**

Внутри `<DndContext>` после `</main>` добавить:

```tsx
<DragOverlay dropAnimation={null}>
  {draggingTask && <TaskDragPreview task={draggingTask} />}
</DragOverlay>
```

Итоговая структура:

```tsx
<DndContext
  sensors={sensors}
  onDragStart={handleDragStart}
  onDragEnd={handleDragEnd}
>
  <main className="max-w-2xl mx-auto px-4 py-6 md:p-8 pb-20">
    {/* ... весь контент ... */}
  </main>
  <DragOverlay dropAnimation={null}>
    {draggingTask && <TaskDragPreview task={draggingTask} />}
  </DragOverlay>
</DndContext>
```

- [ ] **Step 6: Скрыть оригинальный элемент при перетаскивании в SortableTask**

В `src/components/SortableTask.tsx` заменить `opacity: 1` на условную прозрачность:

```tsx
export function SortableTask({ id, children }: { id: string; children: React.ReactNode }) {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
    isDragging,
  } = useSortable({ id })

  return (
    <li
      ref={setNodeRef}
      {...attributes}
      {...listeners}
      style={{
        transform: CSS.Transform.toString(transform),
        transition,
        opacity: isDragging ? 0 : 1,
        touchAction: "manipulation",
      }}
      className="cursor-grab active:cursor-grabbing"
    >
      {children}
    </li>
  )
}
```

Убрать `scale(1.03)` и `boxShadow` из SortableTask — теперь они в `TaskDragPreview`.

- [ ] **Step 7: Проверить в браузере**

Перетащить развёрнутую задачу. Drag-превью должно быть фиксированной высоты (~56px), без растяжения. Оригинальный элемент в списке становится прозрачным (держит место).

- [ ] **Step 8: Commit**

```bash
git add src/app/tasks/page.tsx src/components/SortableTask.tsx
git commit -m "feat: DragOverlay for fixed-height drag preview, hide original while dragging"
```

---

## Task 7: Анимация аккордеонов у ProjectTabs и TagFilter

**Files:**
- Modify: `src/components/projects/ProjectTabs.tsx`
- Modify: `src/components/filters/TagFilter.tsx`

- [ ] **Step 1: Добавить animate-expand в ProjectTabs**

В `src/components/projects/ProjectTabs.tsx` найти expanded-контейнер (~строка 118):

```tsx
{isOpen && (
  <div className="mt-2 bg-white rounded-xl shadow-sm p-3 flex flex-col gap-2">
```

Добавить `animate-expand`:

```tsx
{isOpen && (
  <div className="mt-2 bg-white rounded-xl shadow-sm p-3 flex flex-col gap-2 animate-expand">
```

- [ ] **Step 2: Добавить animate-expand в TagFilter**

В `src/components/filters/TagFilter.tsx` найти expanded-контейнер (~строка 51):

```tsx
{isOpen && (
  <div className="mt-2 bg-white rounded-xl shadow-sm p-3 flex flex-wrap gap-2">
```

Добавить `animate-expand`:

```tsx
{isOpen && (
  <div className="mt-2 bg-white rounded-xl shadow-sm p-3 flex flex-wrap gap-2 animate-expand">
```

- [ ] **Step 3: Проверить в браузере**

Открыть и закрыть «Проекты» и «Метки» — оба должны анимироваться так же как задачи (opacity 0→1 + translateY -4px→0 за 0.18s).

- [ ] **Step 4: Commit**

```bash
git add src/components/projects/ProjectTabs.tsx src/components/filters/TagFilter.tsx
git commit -m "feat: animate-expand on ProjectTabs and TagFilter accordions"
```

---

## Итоговая проверка

- [ ] Запустить все тесты:

```bash
npx vitest run
```

Ожидаемый результат: все тесты PASS (включая новые в `src/lib/dates.test.ts`).

- [ ] Проверить в браузере все 6 функций:
  1. Создать тег — получает случайный цвет из палитры
  2. Фильтры по дате стоят первыми, порядок: Сегодня | Неделя | Потом | Все
  3. Пункты задач выше и плотнее
  4. Задача на сегодня → «сегодня», завтра → «завтра», прошлое → «просрочено»
  5. Перетащить развёрнутую задачу → превью фиксированной высоты
  6. Аккордеоны Проектов и Меток открываются с анимацией
