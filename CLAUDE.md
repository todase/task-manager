@AGENTS.md

## UI Implementation Rule

When implementing any UI component or page that has an HTML mockup in the plan document: **re-read the mockup before writing code and implement to match it exactly.** The mockup is a binding visual spec, not a suggestion.

If no mockup exists, find the most similar existing component in the codebase and use it as the style anchor.
